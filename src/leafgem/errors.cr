Leafgem_errors = {
  "no_renderer" => "Renderer was not initialised but was accessed!
`set_window` needs to be called before any objects are created.
E.g. set_window(\"My game!\", 640, 480, 2, true)",
  "circ_invalid_radius" => "A drawn circle's radius cannot be negative.",
}
